# Small integration check, not the full paper comparison. Fixed before running.
seed: 0
provider: mvp-main
aoi: {lat: 48.2082, lon: 16.3738}  # same coordinates as slo_sweep
fleet_seeds: [0]
n_epochs: 4  # equally spaced over one sidereal day
slos_ms: [300, 1000]
candidate_modes: [matched, vicinity]
vicinity_radius_km: 2000  # upstream config_helper.py
vicinity_count: 60       # upstream config_helper.py
