# E3 planner (EQ3): oracle vs baselines (heuristic and MILP join in Stage 3).
# Devices, prices, links from constants.yaml; workflow + config space from
# workflows/ and ladders/. Placeholder-grid caveat identical to E2.
seed: 0
workflow: wildfire
constellations: [ctrl-star, ref-star, starlink-s1]
aoi: mid_latitude
capable_fractions: [0.002, 0.01, 0.05]
slos_s: [0.6, 0.85, 1.0, 2.0, 4.0]
fleet_seeds: [0, 1, 2]
candidates_k: 3
budget: .inf
random_draws: 5        # random-feasible draws per instance x SLO
milp_slos_s: [0.85, 2.0]   # MILP joins on these SLOs (runtime column; equality
                           # with enumeration is already pinned by tests)
