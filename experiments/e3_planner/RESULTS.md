# E3 Results

**Verdict: PENDING** (C3 planner: heuristic near the MILP oracle, above the
baselines).

Optimality gap (heuristic vs MILP) and the margin over latency-only,
accuracy-only, topology-unaware, and random baselines go here once
`just run e3_planner` produces them.
